[package]
name = "awctl"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "AWC command-line boundary: command parsing and result rendering."

[dependencies]
awc-core = { path = "../awc-core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
